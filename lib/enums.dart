// Defines Enumerators used in the app

// for Bottom nav bar menu state
enum MenuState { home, search, cart, profile }

// for dynamic category screen
enum Category {burger, pizza, fries, chicken, sandwich, icecream, drinks, thali}
